import styles from '@/src/features/account/components/resume/resume.module.scss';

export const ResumeComponent = () => {
  return (
    <div className={styles.resume}>
      <div>progress blabla</div>
    </div>
  );
};
